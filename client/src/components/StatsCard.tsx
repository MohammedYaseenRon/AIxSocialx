import React from 'react'


interface Stats {
    id: string;
    title:string;
    description: string;
    icon: React.ReactNode;
}

const StatsCard = ({id, title, description, icon}: Stats) => {
  return (
    <div className=''>

    </div>
  )
}

export default StatsCard